[pytest]
pythonpath = ..
testpaths = tests
markers =
    integration: exercises the local backend/frontend stack as separate processes.
    real_audio: exercises Whisper with the real audio file from the project root.
